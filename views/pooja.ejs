<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Pooja Master – Shree Dongerkery Venkatramana Temple</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Google Material Icons -->
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; }

    body {
      font-family: 'Segoe UI', sans-serif;
      background: linear-gradient(to bottom right, #fffaf0, #f9f5ed);
      color: #333;
      display: flex;
      flex-direction: column;
      min-height: 100vh;
    }

    header {
      background: linear-gradient(to right, #5d2800, #a85d00);
      color: white;
      padding: 26px 20px;
      text-align: center;
      font-size: 26px;
      font-weight: bold;
      letter-spacing: 0.05em;
      box-shadow: 0 6px 20px rgba(0,0,0,0.2);
    }

    nav {
      background: #fffdf7;
      padding: 14px 20px;
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
      border-bottom: 1px solid #e5dac7;
      box-shadow: 0 4px 8px rgba(0,0,0,0.04);
    }

    nav a {
      text-decoration: none;
      font-weight: 600;
      color: #994d00;
      padding: 8px 16px;
      border-radius: 8px;
      transition: 0.3s;
      font-size: 15px;
    }

    nav a:hover {
      background-color: #ffeacc;
      box-shadow: 0 0 6px rgba(200,100,0,0.2);
    }

    main {
      max-width: 1000px;
      width: 95%;
      margin: 32px auto;
      padding: 30px;
      background-color: white;
      border-radius: 18px;
      box-shadow: 0 12px 40px rgba(0,0,0,0.07);
    }

    h2 {
      text-align: center;
      margin-bottom: 30px;
      color: #8b4000;
      font-size: 24px;
      font-weight: 600;
    }

    form.add-pooja {
      display: flex;
      flex-wrap: wrap;
      justify-content: center;
      gap: 16px;
      margin-bottom: 35px;
    }

    .add-pooja input {
      padding: 12px 14px;
      border: 1.5px solid #ecccb4;
      border-radius: 8px;
      transition: all 0.3s;
      font-size: 15px;
      background: #fff9f1;
      width: 200px;
    }

    .add-pooja input:focus {
      border-color: #ca802c;
      outline: none;
      background: #fff2de;
    }

    .add-pooja button {
      background: linear-gradient(to right, #e28743, #b85008);
      color: white;
      border: none;
      padding: 12px 20px;
      border-radius: 8px;
      font-weight: bold;
      font-size: 15px;
      cursor: pointer;
      box-shadow: 0 4px 10px rgba(0,0,0,0.2);
      transition: 0.3s ease;
    }

    .add-pooja button:hover {
      background: linear-gradient(to right, #e97b0f, #964200);
    }

    table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0 12px;
    }

    th {
      background: #ffe0ba;
      color: #6a3300;
      padding: 12px;
      font-weight: 600;
      text-align: center;
      border-radius: 8px 8px 0 0;
    }

    td {
      background: #fffdfc;
      padding: 12px;
      text-align: center;
      border-radius: 10px;
      box-shadow: 0 2px 5px #ded2bd70;
    }

    td form {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
    }

    input[type="number"] {
      width: 90px;
      padding: 7px;
      border-radius: 6px;
      border: 1px solid #c8c2b5;
      text-align: center;
      font-size: 14px;
      background: #fffaf4;
    }

    .btn {
      display: inline-flex;
      align-items: center;
      gap: 6px;
      padding: 7px 14px;
      border-radius: 8px;
      font-size: 14px;
      font-weight: 600;
      cursor: pointer;
      border: none;
      transition: 0.2s ease;
    }

    .update-btn {
      background: #007d7d;
      color: white;
    }

    .update-btn:hover {
      background: #005f5f;
    }

    .delete-btn {
      background: #c0392b;
      color: white;
    }

    .delete-btn:hover {
      background: #a0281f;
    }

    .toggle-btn {
      background: #f0e6df;
      color: #444;
    }

    .toggle-btn:hover {
      background: #ded6cd;
    }

    .material-icons {
      font-size: 18px;
      vertical-align: middle;
    }

    .back-link {
      margin-top: 30px;
      text-align: center;
    }

    .back-link a {
      text-decoration: none;
      font-weight: 600;
      color: #633b00;
      font-size: 15px;
    }

    .back-link a:hover {
      text-decoration: underline;
    }

    footer {
      background-color: #f8f1e8;
      text-align: center;
      padding: 16px;
      font-size: 14px;
      color: #555;
      margin-top: 40px;
      border-top: 1px solid #e0d5c0;
    }

    @media (max-width: 600px) {
      .add-pooja input {
        width: 100%;
      }

      table, th, td {
        font-size: 13px;
      }

      form.add-pooja {
        flex-direction: column;
        align-items: center;
      }
    }
  </style>
</head>
<body>

  <header>
    🛕 Shree Venkatramana Temple – Pooja Master
  </header>

  <nav>
    <a href="/dashboard">📊 Dashboard</a>
    <a href="/billing">💳 Billing</a>
    <a href="/pooja-master">📘 Pooja Master</a>
    <a href="/report">📁 Reports</a>
    <a href="/collections">💰 Collections</a>
    <a href="/logout">🔓 Logout</a>
  </nav>

  <main>
    <h2>Manage Pooja Services</h2>

    <form method="POST" action="/pooja-master/add" class="add-pooja">
      <input name="pooja_name" placeholder="Pooja Name" required>
      <input name="price" type="number" min="1" step="1" placeholder="₹ Price" required>
      <button type="submit">➕ Add Pooja</button>
    </form>

    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Name</th>
          <th>Price & Update</th>
          <th>Status</th>
          <th>Delete</th>
        </tr>
      </thead>
      <tbody>
        <% poojas.forEach(p => { %>
          <tr>
            <td><%= p.id %></td>
            <td><%= p.pooja_name %></td>
            <td>
              <form method="POST" action="/pooja-master/update/<%= p.id %>">
                <input name="price" type="number" min="1" step="1" value="<%= p.price %>" />
                <button class="btn update-btn" title="Update">
                  <span class="material-icons">autorenew</span> Update
                </button>
              </form>
            </td>
            <td>
              <form method="POST" action="/pooja-master/toggle/<%= p.id %>">
                <button class="btn toggle-btn" title="<%= p.visible ? 'Hide' : 'Unhide' %>">
                  <span class="material-icons"><%= p.visible ? 'visibility' : 'visibility_off' %></span>
                  <%= p.visible ? 'Visible' : 'Hidden' %>
                </button>
              </form>
            </td>
            <td>
              <form method="POST" action="/pooja-master/delete/<%= p.id %>">
                <button class="btn delete-btn" onclick="return confirm('Delete <%= p.pooja_name %>?')">
                  <span class="material-icons">delete</span> Delete
                </button>
              </form>
            </td>
          </tr>
        <% }) %>
      </tbody>
    </table>

    <div class="back-link">
      <a href="/billing">← Back to Billing</a>
    </div>
  </main>

  <footer>
    © <%= new Date().getFullYear() %> Shree Venkatramana Temple | Designed by Sagar 
  </footer>

</body>
</html>
